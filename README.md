# Release Projects

This repository stores release-ready AstrBot plugin projects.

## Projects

- `astrbot_plugin_chat_archive_release/` - AstrBot chat archive plugin release package.

Each project directory is intended to be copied or linked into `AstrBot/data/plugins`
under the plugin's runtime directory name. See the package README for dependency,
configuration, and standalone WebUI notes.
